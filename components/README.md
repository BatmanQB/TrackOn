# Components

Специфікації UI-компонентів: кнопки, поля, картки записів, таймер, списки,
перемикач light/dark, селектор періоду. Стани (default/hover/active/disabled),
адаптив (mobile → desktop), доступність.
